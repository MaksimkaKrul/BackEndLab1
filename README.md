# Лабораторна робота №3: REST API з Базою Даних, ORM та Валідацією

Цей проект є REST API для обліку витрат, розробленим на Flask. У рамках третьої лабораторної роботи функціонал було розширено: додано інтеграцію з базою даних PostgreSQL, використано SQLAlchemy ORM для роботи з даними, впроваджено валідацію вхідних даних за допомогою Marshmallow та реалізовано індивідуальне завдання.

## 🚀 Задеплоєний сервіс

Сервіс розгорнуто на Render.com і доступний за наступною адресою:

*   **Основний URL:** `https://your-app-name-lab3.onrender.com`

## 📋 Варіант індивідуального завдання

*   **Номер групи:** 34
*   **Остача від ділення на 3:** `34 % 3 = 1`
*   **Призначений варіант:** Валюти

Реалізовано функціонал для створення валют. Кожен користувач може мати валюту за замовчуванням. При створенні запису про витрати, якщо валюта не вказана, автоматично підставляється валюта користувача за замовчуванням.

## 🛠️ Технології

*   **Python 3.11**
*   **Flask** та **Flask-Smorest** для створення API та генерації документації.
*   **PostgreSQL** як реляційна база даних.
*   **Flask-SQLAlchemy** як ORM для взаємодії з базою даних.
*   **Flask-Migrate** для управління міграціями схеми бази даних.
*   **Marshmallow** для серіалізації та валідації даних.
*   **Gunicorn** як WSGI-сервер для запуску додатку.
*   **Docker** та **Docker Compose** для контейнеризації та локального запуску.
*   **Render.com** для хмарного деплою.

## ⚙️ Локальний запуск

Для запуску проекту на вашому локальному комп'ютері вам знадобляться встановлені **Docker** та **Docker Compose**.

1.  **Клонуйте репозиторій та перейдіть у потрібну гілку:**
    ```bash
    git clone https://github.com/your-username/your-repo.git
    cd your-repo
    git checkout lab3
    ```

2.  **Запустіть проект за допомогою Docker Compose:**
    Ця команда збере образ, запустить контейнери з додатком та базою даних, і застосує всі необхідні міграції.
    ```bash
    docker-compose up --build
    ```

3.  **Перевірка:**
    *   Додаток буде доступний за адресою `http://localhost:5000`.
    *   **Інтерактивна документація API (Swagger UI)** доступна за адресою: `http://localhost:5000/swagger-ui`.

## 🧪 Тестування (Postman)

У репозиторії знаходиться файл колекції Postman для тестування API.
1.  Імпортуйте колекцію в Postman.
2.  Налаштуйте середовище (Environment) `Local` зі змінною `base_url`, що має значення `http://localhost:5000`.
3.  Колекція містить запити для всіх ендпоінтів, включаючи функціонал для роботи з валютами.

## 📖 API Документація

Повна та інтерактивна документація генерується автоматично і доступна за адресою `/swagger-ui`.

### Основні сутності:
*   **Users** (`/user`): Створення, отримання, оновлення та видалення користувачів.
*   **Categories** (`/category`): Створення, отримання та видалення категорій витрат.
*   **Records** (`/record`): Створення та отримання записів про витрати з можливістю фільтрації.
*   **Currencies** (`/currency`): Створення та отримання списку доступних валют.