# Лабораторна робота №1: Flask Healthcheck API

Цей проект є базовим REST API на Flask з ендпоінтом `/healthcheck`, запакованим у Docker та задеплоєним на Render.com.

## 🚀 Задеплоєний сервіс

Посилання на робочий ендпоінт: [https://backendlab1-6h00.onrender.com/healthcheck](https://backendlab1-6h00.onrender.com/healthcheck)


## 🛠️ Локальний запуск

Для запуску проекту локально вам потрібні встановлені `Docker` та `docker-compose`.

1.  **Клонуйте репозиторій:**
    ```bash
    git clone https://github.com/your-username/flask-healthcheck-lab.git
    cd flask-healthcheck-lab
    ```

2.  **Запустіть за допомогою Docker Compose:**
    ```bash
    docker-compose up --build
    ```

3.  **Перевірка роботи:**
    Додаток буде доступний за адресою `http://localhost:5000`.
    - Головна сторінка: `http://localhost:5000/`
    - Healthcheck ендпоінт: `http://localhost:5000/healthcheck`

## ⚙️ Структура ендпоінтів

### GET /healthcheck

Перевіряє стан сервісу.

- **Успішна відповідь (200 OK):**
  ```json
  {
    "status": "OK",
    "timestamp": "2025-11-13T11:05:00.123456"
  }